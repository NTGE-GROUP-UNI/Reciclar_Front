export interface IStudent {
    id: string;
    fullName: string;
    className: string
    expirationYear: number,
    createdAt: string,
    updatedAt: string,
    status: string,
    absences: number
}